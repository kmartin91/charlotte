import React from "react";
import classnames from "classnames";
import "./Third.scss";

const Third = ({ history }) => {
  return (
    <div className={classnames("Third")}>
      <div className="Third__items">Todo </div>
    </div>
  );
};

export default Third;
